# GitHub Actions DISABLED — build and deploy handled by Azure DevOps pipeline
# See: azure-pipelines.yml

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Disabled — use Azure DevOps'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled"
